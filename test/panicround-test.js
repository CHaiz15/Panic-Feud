import chai from 'chai';
const expect = chai.expect;

import PanicRound from '../src/scripts/PanicRound.js';

describe('PanicRound', () => {
  it('should be a function', function() {
  expect(PanicRound).to.be.a('function');
});
})
